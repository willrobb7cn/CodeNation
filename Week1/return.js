// function addUp(num1, num2) {
//     return num1 + num2;
//     }
    
//     addUp(7,3);
//     // console.log(addUp(2,5));
/*
    const multiplyByNineFifths = (celsius) => {
        return celsius * (9/5);
        };
        
        const getFahrenheit = (celsius) => {
        return multiplyByNineFifths(celsius) + 32;
        };

        console.log(getFahrenheit(18)) // for the return to be usefull it needs to be used in conjunction with another function (scope chain)
        */